import { FilterProductorPipe } from './filter-productor.pipe';

describe('FilterProductorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductorPipe();
    expect(pipe).toBeTruthy();
  });
});
